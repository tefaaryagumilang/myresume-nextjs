/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    // Other config options...
    output: {
      // Use "export" to enable static exporting
      export: {},
    },
  };
